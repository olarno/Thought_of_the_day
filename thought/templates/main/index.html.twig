{% extends 'base.html.twig' %}

{% block title %}Hello !
{% endblock %}

{% block body %}
	<h1>Thoughts list</h1>

	<div class="container">
		{% for thought in thoughts %}

			<div class="row">
				<div class="col-1">
					<h3>{{ thought.title }}
					</h3>
				</div>
				<div class="col-1">
					<div class="square" style="background-color: {{thought.colors.color}};" ></div>
                </div>
                <div class="col-10">
                    <p class="text-muted">{{thought.createdAt|date ("D j F Y à H:m") }}</p>
                </div>
			</div>

			<p>{{ thought.content }}</p>


		{% endfor %}
	</div>
{% endblock %}
